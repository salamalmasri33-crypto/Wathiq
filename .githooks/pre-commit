#!/bin/sh

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is not installed. Skipping secret scan."
  exit 0
fi

echo "Running gitleaks before commit..."
gitleaks protect --staged --config .gitleaks.toml
