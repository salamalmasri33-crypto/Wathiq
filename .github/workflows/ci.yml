name: Wathiq CI

on:
  push:
    branches: [ main ]

jobs:
  pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Test
        run: echo "Test placeholder"

      - name: Run GitLeaks Scan
        run: echo "Running GitLeaks scan..."

      - name: Build Step
        run: echo "Building..."

      - name: Deploy Step
        run: echo "Deploying..."
